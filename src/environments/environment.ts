export const environment = {
  production: false,
  apiUrl: 'http://localhost:5174',
  // Google Cloud Console → APIs & Services → Credentials → Maps JavaScript API. Without key, static iframe is shown.
  googleMapsApiKey: ''
};
